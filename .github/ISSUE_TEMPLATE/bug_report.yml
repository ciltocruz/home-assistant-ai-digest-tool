name: Bug report
description: Report a problem with the application. Fill in every section so the issue can be reproduced.
title: "[Bug]: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for reporting. A complete bug report is the fastest way to a fix.
  - type: input
    id: version
    attributes:
      label: Application version
      description: Version shown in the release notes or the container image tag (for example v1.1.2).
      placeholder: "v1.1.2"
    validations:
      required: true
  - type: dropdown
    id: deployment
    attributes:
      label: How is the application running?
      options:
        - Docker container
        - Local development
        - Other
    validations:
      required: true
  - type: textarea
    id: description
    attributes:
      label: What happened?
      description: What did you expect to happen, and what actually happened?
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: Steps to reproduce
      description: Numbered steps that lead to the problem.
      placeholder: |
        1. ...
        2. ...
        3. ...
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: Relevant logs or error messages
      description: Paste redacted logs. Remove tokens, passwords, entity names, and other sensitive values first.
      render: shell
  - type: checkboxes
    id: privacy
    attributes:
      label: Privacy check
      options:
        - label: I removed all secrets, tokens, and personal data from the information above.
          required: true