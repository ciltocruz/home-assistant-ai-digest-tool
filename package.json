{
  "name": "home-assistant-ai-digest-tool",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r build",
    "ci": "pnpm typecheck && pnpm test && pnpm test:focused && pnpm build",
    "test": "vitest run",
    "test:focused": "node scripts/check-focused-tests.mjs",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
